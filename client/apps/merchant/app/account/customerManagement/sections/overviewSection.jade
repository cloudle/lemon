template(name="customerManagementOverviewSection")
  .section
    .heading
      +avatarImageComponent(avatar=avatarUrl alias=name)
      .caption {{name}}
      .sub1 {{phone}}
      .sub2 {{address}}