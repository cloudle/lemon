template(name="branchManager")
  #header
    .caption-row: .title CHI NHÁNH
    .editor-row
      .editor-wrapper
        span.ilabel tên chi nhánh
        input(name="name" maxlength="40")
      .editor-wrapper(style="width: 300px")
        span.ilabel.optional địa chỉ
        input(name="address")
      .editor-wrapper
        button.lemon.btn.lime.icon-plus-circled#createBranch(class="{{allowCreate}}") THÊM MỚI

  #content
    +iGrid(options=gridOptions)