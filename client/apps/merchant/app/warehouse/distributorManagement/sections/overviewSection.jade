template(name="distributorManagementOverviewSection")
  .section
    .heading
      +avatarImageComponent(avatar=avatarUrl alias=name)
      .caption {{name}}
      .sub1 {{phone}}
      .sub2 {{address}}