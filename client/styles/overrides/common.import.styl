* { font-family: "Open Sans", "Helvetica Neue", Helvetica, sans-serif, Arial }
body { margin: 0 }
a
  cursor pointer
  &:hover { text-decoration: none !important }

input[type="text"]:focus, input[type="password"]:focus { outline: none !important; }